<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenBackground">@color/splashScreenBackground</item>
        <item name="postSplashScreenTheme">@style/Theme.MyNewsApp</item>
        <!--
        this "postSplashScreenTheme" is of use only if the xml theme is being used but in our
        current case we are using the Material theme inside from the compose
        -->
    </style>
</resources>